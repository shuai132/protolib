syntax = "proto3";
package proto;

message AppMsg {
    // 命令类型
    enum Cmd {
        NONE = 0;
        RESERVED = 10;
        // 用户CMD要用10以后的数
        CMD1 = 11;
        CMD2 = 12;
        CMD3 = 13;
        CMD4 = 14;
    }
}
