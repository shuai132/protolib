syntax = "proto3";
package proto;

// 消息收发复用Msg类型
message AppMsg {
    // 命令类型
    enum Cmd {
        NONE = 0;
        RESERVED = 10;
        // todo: add user cmd
        HELLO = 11;
    }

    // todo: add user message
    message HelloPayload {
        string payload = 1;
    }
}
