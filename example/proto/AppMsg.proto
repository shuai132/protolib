syntax = "proto3";
package proto;

// 消息收发复用Msg类型
message AppMsg {
    // 命令类型
    enum Cmd {
        NONE = 0;
        RESERVED = 10;
        // todo: add user cmd
        CMD1 = 11;
        CMD2 = 12;
        CMD3 = 13;
        CMD4 = 14;
    }
}
